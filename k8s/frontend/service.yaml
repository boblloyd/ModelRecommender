apiVersion: v1
kind: Service
metadata:
  name: model-recommender-ui
  namespace: model-recommender
  labels:
    app: model-recommender-ui
spec:
  type: NodePort
  selector:
    app: model-recommender-ui
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30766
